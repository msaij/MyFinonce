services:
  dashboard:
    build:
      context: ./fetcher
      dockerfile: Dockerfile
    container_name: mf_dashboard
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      - DUCKDB_PATH=/app/data/mutual_funds.duckdb
      - TZ=Asia/Kolkata
      - AMFI_DOWNLOAD_PAGE=https://www.amfiindia.com/net-asset-value/nav-download
      - AMFI_HISTORY_URL=https://portal.amfiindia.com/DownloadNAVHistoryReport_Po.aspx
      - AMFI_DAILY_URL=https://portal.amfiindia.com/spages/NAVAll.txt
    volumes:
      - ./fetcher:/app
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8501/_stcore/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3

  # --- Webstack migration (see .claude/plans/floofy-petting-mountain.md) ---
  # backend/frontend run ALONGSIDE dashboard (kept as a legacy reference/
  # fallback), not replacing it yet, until the final cutover phase. backend
  # now runs on its own SQLite file (see app/db/connection.py) -- a clean
  # slate, no data carried over from dashboard's old DuckDB file -- so it
  # owns that file exclusively and is its own sole writer from the start;
  # the AMFI sync re-populates it from scratch (POST /api/admin/sync/* once
  # Phase 9 lands, or trigger amfi_sync directly in the meantime).
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mf_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DB_PATH=/app/data/mutual_funds.sqlite3
      - TZ=Asia/Kolkata
      - AMFI_DOWNLOAD_PAGE=https://www.amfiindia.com/net-asset-value/nav-download
      - AMFI_HISTORY_URL=https://portal.amfiindia.com/DownloadNAVHistoryReport_Po.aspx
      - AMFI_DAILY_URL=https://portal.amfiindia.com/spages/NAVAll.txt
      # false during active development (not "true" as originally set right after the
      # SQLite migration): the daemon does a real, potentially long-running startup
      # catch-up sync (check_and_catchup_sync(), possibly a full historical backfill)
      # against the live AMFI servers, unconditionally, on every container start --
      # discovered when it made the backend unresponsive (even /api/health timing out)
      # for minutes after an ordinary dev rebuild. That's fine for the real, deliberate
      # write-ownership handoff (Phase 9, where this flips true on purpose), not for a
      # container that gets rebuilt many times per hour while iterating. The database
      # already has real synced data from whichever rebuild it last happened to be true
      # for -- this flag only controls the daemon auto-starting, not whether previously
      # synced data is kept.
      - ENABLE_SYNC_DAEMON=false
    volumes:
      - ./backend:/app
      # Read-write: this is backend's own, exclusively-owned data directory
      # (not shared with dashboard/fetcher's data dir at all anymore).
      - ./backend/data:/app/data
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/api/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: mf_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - BACKEND_URL=http://backend:8000
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
