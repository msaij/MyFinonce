fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
# psycopg 3 (not psycopg2): its COPY support is what makes the bulk NAV ingest
# fast, and [pool] provides the connection pool that replaced the old
# thread-local-sqlite3-connection scheme. [binary] ships prebuilt libpq so the
# image needs no build toolchain.
psycopg[binary,pool]>=3.2.0
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0
plotly>=5.20.0
cachetools>=5.3.0
requests>=2.31.0
schedule>=1.2.1
pytz>=2024.1
python-dateutil>=2.9.0
python-multipart>=0.0.9
pytest>=8.0.0
httpx>=0.27.0
